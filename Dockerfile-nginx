FROM nginx:1.25

# Copy site configuration and application files into the image so the
# container does not depend on bind-mounts from the host or Jenkins workspace.
COPY default.conf /etc/nginx/conf.d/default.conf
COPY . /var/www/html

RUN chown -R nginx:nginx /var/www/html || true
